<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RegisterSystem" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:colorControlActivated">@color/dark_blue</item>
        <item name="android:colorControlNormal">@color/dark_blue</item>
    </style>

    <style name="Theme.RegisterSystem" parent="Base.Theme.RegisterSystem" />
</resources>